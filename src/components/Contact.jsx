import { useState } from 'react'
import { FiMail, FiPhone, FiMapPin, FiSend, FiClock } from 'react-icons/fi'
import { FaGithub, FaLinkedin, FaTwitter } from 'react-icons/fa'
import { useSiteData } from '../lib/SiteDataContext'

export default function Contact(){
  const {settings,socials}=useSiteData(); const [formData,setFormData]=useState({name:'',email:'',subject:'',message:'',website:''}); const [status,setStatus]=useState(''); const [statusType,setStatusType]=useState(''); const [submitting,setSubmitting]=useState(false); const [subEmail,setSubEmail]=useState(''); const [subName,setSubName]=useState(''); const [subStatus,setSubStatus]=useState('');
  const socialIcon={github:FaGithub,linkedin:FaLinkedin,twitter:FaTwitter};
  const submit=async(e)=>{e.preventDefault();if(submitting)return;setSubmitting(true);setStatus('Sending...');setStatusType('loading');try{const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(formData)});const text=await r.text();let d={};try{d=text?JSON.parse(text):{}}catch{throw new Error(r.ok?'Invalid server response.':`Server error (${r.status}).`)}if(!r.ok)throw new Error(d.error||`Request failed (${r.status}).`);setStatus(d.message);setStatusType('success');setFormData({name:'',email:'',subject:'',message:'',website:''})}catch(err){setStatus(err.message);setStatusType('error')}finally{setSubmitting(false)}};
  const subscribe=async(e)=>{e.preventDefault();setSubStatus('Subscribing...');try{const r=await fetch('/api/subscribe',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:subName,email:subEmail})});const text=await r.text();let d={};try{d=text?JSON.parse(text):{}}catch{throw new Error(r.ok?'Invalid server response.':`Server error (${r.status}).`)}if(!r.ok)throw new Error(d.error||`Request failed (${r.status}).`);setSubStatus(d.message);if(!d.alreadySubscribed){setSubEmail('');setSubName('')}}catch(err){setSubStatus(err.message)}};
  const info=[['Email',settings.email,`mailto:${settings.email}`,FiMail],['Phone',settings.phone,`tel:${settings.phone.replace(/\s+/g,'')}`,FiPhone],['Location',settings.location,'#contact',FiMapPin],['Available',settings.availabilityHours,'#contact',FiClock]];
  return <section id="contact" className="py-16 sm:py-20 px-4 sm:px-6 section-fade gsap-section overflow-x-hidden"><div className="container mx-auto min-w-0"><div className="text-center mb-10 sm:mb-16 px-1"><h2 className="text-3xl sm:text-4xl md:text-5xl font-bold mb-4 break-words">{settings.contactHeading.split(' ')[0]} <span className="gradient-text">{settings.contactHeading.split(' ').slice(1).join(' ')}</span></h2><div className="w-20 h-1 bg-orange-500 mx-auto rounded-full"/><p className="text-gray-400 mt-4 max-w-2xl mx-auto">{settings.contactDescription}</p></div><div className="grid min-w-0 lg:grid-cols-2 gap-8 lg:gap-12 max-w-5xl mx-auto"><div className="min-w-0"><h3 className="text-2xl font-bold mb-6 text-white">Contact Information</h3><p className="text-gray-400 mb-8">{settings.contactIntro}</p><div className="space-y-4 mb-8">{info.map(([label,value,href,Icon])=><a key={label} href={href} className="flex min-w-0 items-center gap-3 sm:gap-4 glass rounded-xl p-4 card-hover overflow-hidden"><div className="w-12 h-12 bg-orange-500 rounded-xl flex items-center justify-center flex-shrink-0"><Icon className="text-white text-xl"/></div><div className="min-w-0 flex-1"><p className="text-gray-500 text-sm">{label}</p><p className="text-white font-semibold break-words [overflow-wrap:anywhere]">{value}</p></div></a>)}</div><div><p className="text-gray-500 text-sm mb-3">Follow me on:</p><div className="flex gap-3">{socials.map(s=>{const Icon=socialIcon[s.icon]||FaGithub;return <a key={s.id||s.name} href={s.url} target="_blank" rel="noopener noreferrer" aria-label={s.name} className="w-12 h-12 glass rounded-xl flex items-center justify-center text-xl text-gray-400 hover:text-white"><Icon/></a>})}</div></div><div className="glass rounded-2xl p-4 sm:p-6 mt-8 min-w-0 overflow-hidden"><h3 className="text-xl font-bold text-white mb-2">Stay Updated</h3><p className="text-gray-400 text-sm mb-4">Subscribe for new projects and portfolio updates.</p><form onSubmit={subscribe} className="space-y-3"><input value={subName} onChange={e=>setSubName(e.target.value)} placeholder="Your name" className="w-full min-w-0 max-w-full box-border bg-gray-800/50 border border-gray-700 rounded-xl px-4 py-3 text-white"/><input value={subEmail} onChange={e=>setSubEmail(e.target.value)} type="email" required placeholder="you@example.com" className="w-full min-w-0 max-w-full box-border bg-gray-800/50 border border-gray-700 rounded-xl px-4 py-3 text-white"/><button className="w-full py-3 bg-orange-500 rounded-xl text-white font-semibold">Subscribe</button></form>{subStatus&&<p className="text-sm text-orange-300 mt-3">{subStatus}</p>}</div></div><div className="glass rounded-2xl p-4 sm:p-6 md:p-8 min-w-0 overflow-hidden"><h3 className="text-xl sm:text-2xl font-bold mb-6 text-white break-words">Send Me a Message</h3>{status&&<div className={`mb-4 p-4 rounded-xl text-sm border ${statusType==='success'?'bg-green-500/10 border-green-500/30 text-green-400':statusType==='error'?'bg-red-500/10 border-red-500/30 text-red-400':'bg-blue-500/10 border-blue-500/30 text-blue-400'}`}>{status}</div>}<form onSubmit={submit} className="space-y-5">{[['name','Your Name','text'],['email','Your Email','email'],['subject','Subject','text']].map(([name,label,type])=><div key={name}><label className="block text-gray-400 text-sm mb-2">{label}</label><input name={name} type={type} value={formData[name]} onChange={e=>setFormData({...formData,[name]:e.target.value})} required={name!=='subject'} className="w-full min-w-0 max-w-full box-border bg-gray-800/50 border border-gray-700 rounded-xl px-4 py-3 text-white"/></div>)}<div><label className="block text-gray-400 text-sm mb-2">Message</label><textarea name="message" value={formData.message} onChange={e=>setFormData({...formData,message:e.target.value})} required rows={5} className="w-full min-w-0 max-w-full box-border bg-gray-800/50 border border-gray-700 rounded-xl px-4 py-3 text-white resize-none"/></div><input type="text" name="website" value={formData.website} onChange={e=>setFormData({...formData,website:e.target.value})} className="hidden" tabIndex={-1} autoComplete="off"/><button disabled={submitting} className="w-full min-w-0 flex items-center justify-center gap-2 py-4 px-4 bg-orange-500 rounded-xl text-white font-semibold disabled:opacity-60"><FiSend/>{submitting?'Sending...':'Send Message'}</button></form></div></div></div></section>
}
